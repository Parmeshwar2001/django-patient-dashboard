# 🩺 Django Patient Dashboard (MongoDB + Bootstrap + Chart.js)

A data analytics dashboard built using **Python Django**, **MongoDB Compass**, and **Chart.js**.  
It visualizes patient demographics (gender, state, and birthdate) using interactive charts and filters.

## 🔧 Features
- Load patient data from CSV into MongoDB
- View data on a Bootstrap dashboard
- Filter by **State**, **Gender**, **City**, or **Birthdate range**
- Export filtered results to CSV
- Real-time chart updates (Chart.js)

## ⚙️ Tech Stack
- Python Django
- MongoDB (Compass)
- HTML, CSS, JS, Bootstrap
- Chart.js for analytics

## 🚀 How to Run
```bash
git clone https://github.com/<your-username>/django-patient-dashboard.git
cd django-patient-dashboard
pip install -r requirements.txt
python manage.py runserver
